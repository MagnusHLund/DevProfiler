<?php

// Exit if accessed directly
if (! defined('ABSPATH')) {
    exit;
}

// Later functionality.
// When document ready is triggered in the frontend
// It calls the API to pause the profiler, if configured.